{
  "name": "financeanchor",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "build": "pnpm -r --if-present build",
    "dev:api": "pnpm --filter @financeanchor/api dev",
    "db:generate": "pnpm --filter @financeanchor/api db:generate",
    "db:migrate": "pnpm --filter @financeanchor/api db:migrate",
    "db:migrate:remote": "pnpm --filter @financeanchor/api db:migrate:remote",
    "db:seed": "pnpm --filter @financeanchor/api db:seed"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.6",
    "eslint": "^10.11.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "prettier": "^3.9.9",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.1",
    "vitest": "~4.1.11"
  }
}
